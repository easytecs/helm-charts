apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kmind-client.fullname" . }}-test-connection"
  labels:
    {{- include "kmind-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kmind-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
